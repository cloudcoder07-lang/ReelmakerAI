package com.reelmakerai.release

object ChangelogGenerator {

    fun generate(): String {
        return """
            • Cinematic export transitions
            • Offline mode and fallback UX
            • Voice FX memory restoration
            • Multilingual branding and SEO tags
            • Share + referral flow
            • Unlock telemetry and growth dashboard
        """.trimIndent()
    }
}
